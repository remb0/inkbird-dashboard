.DS_Store
._*
Thumbs.db
*.swp

# The probe artwork lives in www/ (vendored under upstream's MIT licence).
# This only stops a stray copy at the repo root being committed by accident.
/int12e-probe-*.png
